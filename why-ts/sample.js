// function sum(a, b) {
//     return a + b;
// }

// var result = sum(10, 20);
// result.toLocaleString();
// // result . 에 메소드가 자동완성이 되지 않음

// @ts-check
// typescript 처럼 체크하는 방식

/**
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */

function sum(a, b) {
    return a + b;
}

sum(10, "20");
